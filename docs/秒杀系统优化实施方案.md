Edge Function 部分已更新为完整实现。主要改进点：

改进	说明
三段式整合	预校验 → 请求合并 → 三层限流，一个入口全部处理
签名校验	HMAC 签名防重放，防止伪造请求绕过 CDN
滑动窗口限流	基于边缘 KV 原子递增，IP/用户/SKU 三级
请求合并	50ms 窗口内重复请求在边缘拦截，并清除标记
异常降级	边缘出错时自动降级回源，不影响业务
源站 KV 同步	@Scheduled 定时任务每 5 秒推送活动信息到边缘 KV
多平台部署	支持阿里云 EdgeRoutine / 腾讯云 EdgeOne / Cloudflare Workers